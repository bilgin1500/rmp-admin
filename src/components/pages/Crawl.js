import React from 'react';
import Helmet from 'react-helmet';

const Crawl = () => (
  <div>
    <Helmet title="Crawl" />
    <h2>Crawl</h2>
    <p>Ready to fetch some data?</p>
  </div>
);

export default Crawl;
